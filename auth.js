import firebase from "firebase"
import StyledFirebaseAuth from "firebaseui"

state = { isSignedIn = false }
render() {
    return ( <
        div className = "App" >
        <
        <
        /div>
    )
}